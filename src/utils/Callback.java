package utils;

/**
 * A simple callback interface with only one method: {@link #callback()}.
 *
 * @author 蔡辉宇
 */
public interface Callback {
    /**
     * Logic for the callback implementation.
     */
    void callback();
}
